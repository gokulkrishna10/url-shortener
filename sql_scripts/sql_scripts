CREATE TABLE url_mappings (
  uniq_id SERIAL PRIMARY KEY,
  shortUrl VARCHAR(100) NOT NULL UNIQUE,
  longUrl VARCHAR(450) NOT NULL,
  token VARCHAR(45) NOT NULL UNIQUE,
  urlHash VARCHAR(100) NOT NULL UNIQUE,
  createdDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);